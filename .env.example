# Local Codex subscription auth is stored by the Codex CLI.
# Run:
#   codex login
#   codex login status
#
# No API key is required for local ChatGPT subscription use.

# Optional: per-stage model overrides (otherwise config/stages.yaml wins)
# HARNESS_MODEL_HUNT=gpt-5.4
# HARNESS_MODEL_VALIDATE=gpt-5.5
